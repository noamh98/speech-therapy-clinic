Build the project and deploy to production Firebase Hosting.

Steps:
1. Run `npm run build` — stop and report if there are any errors
2. Run `firebase deploy --only hosting`
3. Confirm success and print the live URL: https://speech-therapy-clinic.web.app

If the build fails, show the error output and do NOT proceed with the deploy.
At the end, show a one-line summary: build status + live URL.
